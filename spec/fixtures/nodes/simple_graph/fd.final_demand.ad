# Just an example of a converter that supposedly uses 100% of the residential
# final energy demand use for natural gas.

- input.coal = 0.5
- input.corn = 0.5
- output.loss = elastic
- graph_methods = [conversion_setter]

~ demand =
  EB(residential, natural_gas) * 1.00
